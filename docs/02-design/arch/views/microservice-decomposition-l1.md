# UPF L1 级微服务划分 (Container View)

## 1. 微服务拆分矩阵 (R2A Mapping)
本矩阵建立了微服务 (L1) 与特性需求 (IR) 的映射关系。

| 微服务名称 | 承载的 IR 列表 | 职责描述 | 技术栈 | 划分理由 |
| :--- | :--- | :--- | :--- | :--- |
| **UPF-C (Control Plane)** | `IR.UPF.FUN.001` (会话管理), `IR.UPF.FUN.002` (计费上报) | 负责 N4 接口 PFCP 协议解析、会话状态机维护、QoS/计费策略分发、统计汇总与上报。 | Go / Gin / PFCP-Lib | 业务逻辑复杂，需高并发处理信令，Go 语言在微服务治理和协议栈开发中具有优势。 |
| **UPF-D (Data Plane)** | `IR.UPF.PERF.001` (高性能转发), `IR.UPF.FUN.001.001` (GTP-U 解封装) | 负责 N3/N6 接口报文的高性能转发、隧道封解包、流分类匹配 (PDR)、数据面统计。 | C / VPP / DPDK | 性能敏感，需矢量化报文处理能力，C/VPP 在数据面吞吐和时延方面表现卓越。 |

## 2. 服务间通信接口 (Internal Interface)
- **协议**: VPP Binary API (配置下发) / VPP Stats Segment (统计数据拉取)。
- **规范**: 遵循 `docs/02-design/arch/intf-internal/001-cp-dp-urr-interface.md`。

## 3. 故障隔离与弹性伸缩
- **隔离性**: UPF-C 故障不影响已建立会话的存量报文转发。
- **弹性**: UPF-D 可根据流量负载（PPS/BPS）独立进行水平扩容（利用 SR-IOV/DPDK 结合 K8s HPA）。

---
*Generated by Gemini CLI based on svc-decomp v0.2 Standards.*
