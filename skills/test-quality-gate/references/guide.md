# 质量门控 (Quality Gate) 准出红线规范 (专家版 v1.0)

## 1. 单元测试 (UT) 出口红线
- **通过率**: 100% 成功。
- **覆盖率**:
    - **整体代码**: 语句覆盖率 > 80%。
    - **P0 核心逻辑 (GTP/PFCP)**: 语句覆盖率必须达到 100%。
- **白盒安全**:
    - C 代码必须通过 Valgrind 或 ASan 扫描，结果为 **0 内存泄漏**。
    - `cppcheck`/`clang-tidy` 静态扫描 **0 告警**。

## 2. 集成测试 (IT) 出口红线
- **接口覆盖**: 100% 覆盖 `spec-rtm.md` 中定义的组件间接口 (N4, Intra-UPF)。
- **一致性**: 必须验证所有定义的错误返回码路径 (Error Paths)。

## 3. 系统测试 (ST) 出口红线
- **性能基准**: 
    - 吞吐量需达到设计目标的 95% 以上。
    - 转发面抖动 (Jitter) 需控制在微秒级。
- **鲁棒性**:
    - 必须通过 DFMEA 中识别的所有 P0 级失效模式模拟测试。
    - 故障自愈时间 (Pod Recovery) < 3s。

## 4. 物理记录准则
- 所有的测试结果必须关联对应的 **AR/SR ID**。
- 只有满足上述 100% 红线指标，TR5 报告方可签署 "Go"。

---
*Generated by Skill Manager. 确保 5G 网元的极致交付质量。*
